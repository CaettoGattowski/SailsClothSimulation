#version 330 core
in vec3 normal;
in vec2 uv;
out vec4 fragColor;

void main()
{
    float light = clamp(dot(normalize(normal), vec3(0.3, 0.7, 0.2)), 0.1, 1.0);
    fragColor = vec4(vec3(0.9, 0.9, 0.85) * light, 1.0);
}
